import type { Store } from "../../../shared/store";
import type { ChessboardState } from "../state";

export function mountPanel(
  _el: HTMLElement,
  _store: Store<ChessboardState>,
  _onChange: () => void,
): void {
  // Filled in Task 10.
}
